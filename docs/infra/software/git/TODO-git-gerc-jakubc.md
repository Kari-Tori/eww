# TODO: Konfiguracja i dokumentacja GIT (GERC, jakubc)

- [ ] Uzupełnić szczegółową konfigurację git (`~/.gitconfig`, aliasy, hooks) — @jakubc
- [ ] Zweryfikować SSH key i dostęp do GitHub — @jakubc
- [ ] Opisać workflow commit/push/pull dla GERC — @jakubc
- [ ] Dodać przykłady rozwiązywania konfliktów — @jakubc
- [ ] Opisać integrację z Obsidian Git plugin — @jakubc
